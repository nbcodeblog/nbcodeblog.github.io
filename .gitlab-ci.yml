pages:
  stage: deploy
  image: alpine:latest
  before_script:
    - apk add --no-cache rsync
  script:
    - mkdir -p public
    - rsync -av --exclude=public ./ public/
  artifacts:
    paths:
      - public
  only:
    - main